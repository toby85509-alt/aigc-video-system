# 电商场景 AIGC 带货视频生成系统 — 项目成果提交文档

---

## 一、基础信息

| 字段 | 内容 |
|------|------|
| **项目名称/课题** | 电商场景 AIGC 带货视频生成系统 |
| **参赛课题** | AI 全栈挑战赛 — 电商场景 AIGC 带货视频生成系统 |
| **团队名称** | （请填写团队名称） |
| **团队成员** | 范文昊（前端开发与测评）、柯淏睿（后端开发与代码架构） |
| **一句话核心业务价值** | 面向 TikTok Shop 商家的端到端智能带货短视频生成系统，实现从素材入库、AI 剧本生成到一键成片的完整闭环，降低商家视频制作门槛，提升内容生产效率与转化效果。 |

### 分工说明

| 成员 | 角色 | 负责模块 | 具体内容 |
|------|------|---------|---------|
| **范文昊** | 前端开发与测评 | 客户端全栈 + 系统评测 | React 页面开发（Dashboard / Materials / ScriptEditor / VideoStudio / TaskCenter / QuickCreate / Methodology）、组件库（Layout / MaterialUploader / StoryboardEditor / VideoPreview / ProgressTracker / Skeleton / Toast）、状态管理（AppContext）、API 客户端封装、响应式布局与主题系统、端到端功能评测与样例验证 |
| **柯淏睿** | 后端开发与代码架构 | 服务端全栈 + 架构设计 | Express API 路由设计（materials / scripts / videos / tasks / dashboard / agent / compliance / references / traces）、Service 层业务逻辑（素材管理、剧本生成、视频渲染管线、Agent 编排、合规审核、Trace 追踪）、火山引擎/DeepSeek SDK 集成与 Prompt 工程、ffmpeg 视频合成、中间件（错误处理、安全鉴权、请求日志）、CI/CD Pipeline、代码质量规范（ESLint/Prettier/Husky） |

---

## 二、功能说明

### 核心功能清单（P0/P1/P2 全部完成）

| 序号 | 功能 | 级别 | 说明 |
|------|------|------|------|
| 1 | **素材管理** | P0 | 支持商品主图/视频上传（多文件、大文件）、AI 自动标签与多颗粒度切片分析、关键词+标签+类目检索 |
| 2 | **AI 剧本生成** | P0 | 3 套创作模板 + 多因子解耦引擎，融合商品信息生成含叙事框架/分镜/台词/字幕/BGM 的完整剧本；支持 A/B 多模板并行出本 |
| 3 | **一键成片** | P0 | 端到端视频生成：素材→剧本→图生视频/文生视频→ffmpeg 合成→导出；支持竖版 9:16 与横版 16:9 |
| 4 | **分镜级编辑器** | P1 | 可视化分镜列表 + 详情编辑面板，支持拖拽排序、单分镜 AI 指令重生成、素材切片替换、时长/台词/转场编辑 |
| 5 | **任务进度与 Trace** | P0/P1 | 异步长任务实时进度追踪（步骤级）、生成全链路 Trace 记录（模型端点/耗时/质量分/兜底信息） |
| 6 | **Agent 管线** | P2 | 6 Agent 编排管线：素材分析→视觉匹配→智能剪辑→音频合成→质量审核；支持项目级启动与状态追踪 |

### 端到端使用流程

1. **素材入库**：商家进入素材管理页，上传商品主图/视频，填写商品标题、类目、卖点等信息，系统自动调用火山引擎多模态模型进行素材分析，生成标签与切片描述，资产状态变为"就绪"。
2. **剧本生成**：进入剧本编辑页，输入商品信息（标题、卖点、目标人群、使用场景），选择创作模板（质感沉浸风/快节奏种草风/问题解决型），系统调用 LLM 生成完整分镜脚本（叙事框架 + 4 组分镜描述 + 镜头运动 + 台词 + 字幕 + BGM）。支持一键生成 A/B 三套模板对比。
3. **剧本干预**：在分镜编辑器中查看每个分镜的画面描述、运镜方式、时长、旁白与字幕，可拖拽调整顺序、修改台词，或输入自然语言指令对单个分镜进行 AI 重生成。
4. **视频创作**：选择剧本与关联素材，设定分辨率（9:16/16:9），一键启动视频生成。系统按分镜依次调用图生视频/文生视频接口，生成各分镜片段后通过 ffmpeg 合成完整视频。
5. **任务监控**：在任务中心实时查看生成进度（分步骤展示：素材分析→分镜生成→视频渲染→音频合成→质量审核），失败分镜支持一键重渲染，异常任务展示详细错误信息。
6. **效果分析**：在工作台查看风格×转化矩阵、多因子归因分析、A/B 测试历史与热力图，为商家提供数据驱动的选品与内容策略参考。

---

## 三、交付材料

| 材料 | 链接/说明 |
|------|----------|
| **在线 Demo** | （请填写可访问的 Demo 链接；本地启动见下方运行说明） |
| **演示视频** | （请上传至公开平台后填写链接，建议 3-8 分钟，覆盖素材→剧本→成片→分析全流程） |
| **源代码仓库** | [https://github.com/toby85509-alt/aigc-video-system](https://github.com/toby85509-alt/aigc-video-system) |
| **README/运行说明** | 见下方 |

### 运行说明

**环境要求**：Node.js ≥ 18、npm ≥ 9

**1. 安装依赖**
```bash
npm run install:all
```

**2. 配置环境变量**
复制 `.env.example` 为 `.env`，按需填入：
- `VOLCANO_ARK_API_KEY` / `VOLCANO_TEXT_EP` / `VOLCANO_VIDEO_EP`：火山引擎方舟 API 配置
- `DEEPSEEK_API_KEY`：DeepSeek API Key（兜底模型）
- `MOCK_AI=true`：无真实 API Key 时使用 Mock 模式，系统本地生成剧本并模拟视频渲染

**3. 启动开发环境**
```bash
npm run dev           # 同时启动前后端
# 后端 → http://localhost:3001
# 前端 → http://localhost:5173
```

**4. 验证命令**
```bash
npm run lint          # ESLint 检查
npm run build         # 构建
```

**5. 使用路径**
1. 素材管理 → 上传商品图片/视频
2. 剧本编辑 → 输入商品信息 → 选择模板 → AI 生成分镜剧本
3. 视频创作 → 选择剧本与素材 → 一键成片
4. 任务中心 → 查看进度与 Trace
5. 工作台 → 查看风格归因分析

**目录结构**
```
aigc-video-system/
├── server/src/
│   ├── index.ts                 # Express 服务入口
│   ├── config/env.ts            # 环境变量加载
│   ├── routes/                  # API 路由层（9 组路由）
│   │   ├── materials.ts         # 素材 CRUD + 上传
│   │   ├── scripts.ts           # 剧本生成 + 分镜干预
│   │   ├── videos.ts            # 视频项目 + 渲染
│   │   ├── tasks.ts             # 任务追踪
│   │   ├── dashboard.ts         # 看板指标
│   │   ├── agent.ts             # Agent 管线
│   │   ├── compliance.ts        # 合规审核
│   │   ├── references.ts        # 参考爆款拆解
│   │   └── traces.ts            # 生成 Trace
│   ├── services/                # 业务逻辑层
│   │   ├── volcano.service.ts   # 火山引擎/DeepSeek SDK
│   │   ├── material.service.ts  # 素材管理
│   │   ├── script.service.ts    # 剧本生成引擎
│   │   ├── video.service.ts     # 视频渲染管线
│   │   ├── task.service.ts      # 任务状态机
│   │   ├── agent.service.ts     # Agent 编排
│   │   ├── compliance.service.ts # 合规审核引擎
│   │   └── trace.service.ts     # 全链路追踪
│   ├── middleware/               # 中间件
│   │   ├── error-handler.ts     # 错误处理+请求日志
│   │   └── security.ts          # 访问令牌鉴权
│   └── types/index.ts           # 服务端类型定义
├── client/src/
│   ├── App.tsx                   # 应用入口 + 路由
│   ├── pages/                    # 7 个页面
│   │   ├── Dashboard.tsx         # 工作台（指标/归因/热力图）
│   │   ├── QuickCreate.tsx       # 一键端到端创建
│   │   ├── Materials.tsx         # 素材管理
│   │   ├── ScriptEditor.tsx      # 剧本生成+编辑
│   │   ├── Methodology.tsx       # 方法论知识库
│   │   ├── VideoStudio.tsx       # 视频创作
│   │   └── TaskCenter.tsx        # 任务中心
│   ├── components/               # 通用组件
│   │   ├── Layout.tsx            # 侧边导航布局
│   │   ├── MaterialUploader.tsx  # 素材上传弹窗
│   │   ├── StoryboardEditor.tsx  # 分镜编辑器
│   │   ├── VideoPreview.tsx      # Canvas 视频预览
│   │   ├── ProgressTracker.tsx   # 任务进度追踪
│   │   ├── Skeleton.tsx          # 骨架屏加载态
│   │   └── Toast.tsx             # 全局消息提示
│   ├── context/AppContext.tsx     # 全局状态（Toast/主题）
│   ├── api/client.ts             # API 客户端（9 组接口）
│   ├── types/index.ts            # 前端类型定义
│   └── styles/globals.css        # 全局样式 + 响应式
└── .github/workflows/ci.yml      # CI/CD Pipeline
```

---

## 四、技术说明

### 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    前端展示层 (React 18 + TypeScript)          │
│  Dashboard │ QuickCreate │ Materials │ ScriptEditor          │
│  VideoStudio │ TaskCenter │ Methodology                      │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  组件层: Layout | StoryboardEditor | VideoPreview    │    │
│  │         MaterialUploader | ProgressTracker           │    │
│  │  状态层: AppContext (Toast + Theme)                  │    │
│  │  通信层: API Client (fetch + X-Access-Token)         │    │
│  └─────────────────────────────────────────────────────┘    │
└──────────────────────┬──────────────────────────────────────┘
                       │ REST API (JSON)
┌──────────────────────▼──────────────────────────────────────┐
│               后端服务层 (Express + TypeScript)                │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Middleware: 请求日志 | CORS | 安全鉴权 | 错误处理     │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │ materials│ │ scripts  │ │ videos   │ │ tasks    │      │
│  │ 素材 CRUD │ │ 剧本生成  │ │ 视频管线  │ │ 任务追踪  │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │ dashboard│ │ agent    │ │compliance│ │ traces   │      │
│  │ 数据看板  │ │Agent 管线 │ │ 合规审核  │ │ 全链路追踪│      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Service 层                                           │   │
│  │  material.service | script.service | video.service    │   │
│  │  task.service | agent.service | compliance.service    │   │
│  │  trace.service | volcano.service (AI SDK 封装)         │   │
│  └──────────────────────────────────────────────────────┘   │
└──────┬──────────────────────┬───────────────────────────────┘
       │                      │
┌──────▼──────┐    ┌─────────▼──────────┐
│  火山引擎方舟  │    │  DeepSeek (兜底)    │
│  Seed 2.0 Pro│    │  deepseek-chat     │
│  Seedance 1.5│    │  TTS 语音合成       │
└──────────────┘    └────────────────────┘
       │
┌──────▼──────┐
│  本地存储     │
│  JSON 持久化  │
│  + 文件系统   │
│  + ffmpeg    │
└──────────────┘
```

### 核心技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| **前端** | React 18 + TypeScript + Vite 6 | SPA 应用，React Router v6 路由，lucide-react 图标库 |
| **后端** | Node.js + Express + TypeScript | RESTful API，tsx 热重载开发 |
| **AI 模型** | 火山引擎方舟 Doubao-Seed-2.0-pro（文本/多模态分析）、Seedance-1.5-pro（图生视频/文生视频）、DeepSeek（兜底文本生成 + TTS） | OpenAI 兼容 SDK 统一调用 |
| **视频处理** | ffmpeg (@ffmpeg-installer/ffmpeg) | 分镜视频拼接合成，支持 stream-copy 与重编码双策略 |
| **存储** | 本地文件系统 + JSON 文件持久化 | materials.json / scripts.json / projects.json / tasks.json / pipelines.json / audit_log.json / generation_traces.json |
| **代码质量** | ESLint + Prettier + Husky | 提交前自动格式化和检查 |
| **CI/CD** | GitHub Actions | Push/PR 触发：安装依赖 → ESLint → TypeScript 类型检查 → 构建 |
| **部署** | 本地开发环境（localhost） | 前端 :5173，后端 :3001，Vite proxy 串联 |

### 大模型 / AI 能力使用说明

| 能力 | 模型/服务 | 在系统中的位置 | 说明 |
|------|----------|--------------|------|
| **素材多模态分析** | Doubao-Seed-2.0-pro（火山方舟） | `volcano.service.ts` → `analyzeMaterial()` → `material.service.ts` 上传链路 | 输入商品图片 + 商品信息，输出标签、描述、切片类型；失败时自动回退到本地规则 Mock |
| **剧本结构化生成** | Doubao-Seed-2.0-pro（主）/ DeepSeek（兜底） | `volcano.service.ts` → `generateStructuredJSON()` → `script.service.ts` `generate()` | System Prompt 含 ~40 条约束（叙事结构、分镜规则、合规要求），User Prompt 融合策略+因子+商品信息；API 失败自动触发 `buildFallbackScript()` 本地兜底 |
| **图生视频 (I2V)** | Seedance-1.5-pro（火山方舟） | `volcano.service.ts` → `generateVideoFromImage()` → `video.service.ts` `renderProject()` | 上传商品图作为首帧，生成 3-5 秒分镜视频；支持 --rt（分辨率）、--dur（时长）、--wm（水印）、--rs（随机种子）指令 |
| **文生视频 (T2V)** | Seedance-1.5-pro（火山方舟） | `volcano.service.ts` → `generateVideoFromText()` | 无商品图时的兜底视频生成方案 |
| **TTS 语音合成** | DeepSeek（SSML 标记） | `volcano.service.ts` → `synthesizeSpeech()` | 将旁白文本转为 SSML 标注语音，用于视频配音说明 |
| **分镜 AI 干预** | Doubao-Seed-2.0-pro / DeepSeek | `script.service.ts` → `regenerateShot()` | 用户输入自然语言指令，单分镜独立重生成，不改动其他分镜 |
| **Agent 管线** | 6 Agent 定义（模拟编排） | `agent.service.ts` | 素材分析 Agent → 剧本校验 Agent → 视觉匹配 Agent → 智能剪辑 Agent → 音频合成 Agent → 质量审核 Agent |

### Prompt 策略

**剧本生成 System Prompt 核心约束（~40 条）**：
- 输入策略因子（opening / bgm / cameraStyle / visualFocus / narration / pacing）强制融入分镜设计
- 输出严格 JSON 结构：narrative → visualStyle → shots[4]（description / cameraMovement / duration / narration / subtitle / bgm / transition）→ constraints[]
- 时长约束：总时长 15-20s，单分镜 3.5-5s
- 合规约束：禁止文字/UI/价格/弹窗植入画面描述，`sanitizeVisualDescription()` 后处理强制过滤
- 素材声明：要求约束清单包含素材来源声明
- 8 点质量评估：结构完整性、Hook 强度、卖点展示、信任背书、合规 CTA、无文字诱导、风险可控、来源声明

**视频生成 Prompt 核心约束**：
- 格式约束：竖版 9:16 或横版 16:9
- 商品一致性：保持商品主体形态/颜色/比例不失真
- 反文字指令：多次强调画面中不渲染任何文字、字母、数字、字幕、价格、Logo
- 反 UI 指令：禁止弹窗、按钮、进度条、界面元素
- 运动约束：按照分镜指定的运镜方式（推/拉/摇/移/跟）

### 关键工程难点与解决方案

**1. 视频生成异步长任务的状态同步与失败恢复**

- **难点**：视频生成涉及多分镜 AI 渲染 + ffmpeg 合成，单次任务需数分钟，过程中的状态追踪和失败处理复杂。
- **方案**：
  - 设计 `Task` 状态机模型（queued → running → completed/failed），每个 Task 包含多个 `TaskStep`（分别追踪素材分析、分镜生成、视频渲染等子步骤）。
  - 后端 `video.service.ts` 使用 `setInterval` 轮询火山引擎视频任务状态（每 5s，最长 10min），实时更新 `TaskStep` 进度。
  - 前端 `TaskCenter.tsx` 每 5s 自动刷新，`ProgressTracker` 组件可视化展示步骤级时间线。
  - 单分镜失败不会阻塞整体：`rerenderShot()` 支持独立重渲染后重新合成。

**2. AI 模型调用失败的多层兜底策略**

- **难点**：火山引擎 API 可能存在限流、超时、返回异常 JSON 等问题，端到端链路不能因单点 AI 故障中断。
- **方案**：
  - 三层兜底：火山引擎主模型 → DeepSeek 兜底模型 → 本地规则生成（`buildFallbackScript()`）。
  - 视频生成限流控制：`reserveVideoCreateSlot()` 信号量模式强制 16s 最小调用间隔，429 错误指数退避重试（45s 基数 × 最多 4 次）。
  - JSON 解析容错：`generateStructuredJSON()` 自动去除 markdown code fence 再解析。
  - Mock 模式：`MOCK_AI=true` 时完全离线运行，返回类目感知的模拟数据，保证开发与演示可运行。

**3. AIGC 视频中的文字/UI 幻觉抑制**

- **难点**：图生视频模型倾向于在画面中渲染文字、字幕、价格标签、UI 按钮等电商常见元素，破坏视频观感。
- **方案**：
  - Prompt 层：视频生成 Prompt 中多次、多角度强调"不渲染文字/字母/数字/字幕/价格/Logo/弹窗/按钮/进度条/界面元素"。
  - 后处理层：`sanitizeVisualDescription()` 对剧本中的画面描述进行正则过滤，将任何文字/UI 相关描述替换为安全的视觉等效描述（如 "Title appears" → "Product centered on pedestal"）。
  - 合规审核层：`compliance.service.ts` 对生成内容进行敏感词扫描（虚假宣传、绝对化用语、缺失声明），拦截问题内容。

**4. 分镜级编辑器复杂交互的实现**

- **难点**：分镜列表需支持拖拽排序、单分镜 AI 重生成、局部编辑与实时预览，且不能因单分镜修改触发全片重渲染。
- **方案**：
  - `StoryboardEditor.tsx` 双面板布局：左侧可拖拽分镜列表 + 右侧选中分镜详情编辑表单。
  - 单分镜重生成调用 `scriptsApi.regenerateShot(scriptId, shotId, instruction)`，仅替换目标分镜数据。
  - `VideoPreview.tsx` Canvas 动画预览器：使用 HTML5 Canvas 模拟运镜效果（缩放、平移、旋转），叠加字幕渲染与转场进度条，无需实际视频文件即可预览。

### 部署与访问说明

- **部署方式**：本地开发环境启动，前后端分离
- **前端地址**：`http://localhost:5173`
- **后端地址**：`http://localhost:3001`
- **快速体验路径**：
  1. 配置 `.env` 中 `MOCK_AI=true`（无需真实 API Key 即可运行）
  2. `npm run install:all && npm run dev`
  3. 浏览器打开 `http://localhost:5173`
  4. 按「素材管理 → 剧本编辑 → 视频创作」流程体验完整链路
- **CI/CD**：GitHub Actions 自动执行 Lint + Type Check + Build，提交即触发

---

## 五、结果说明

### 项目完成度

**可用 Demo 版本**：P0（必做）全部完成，P1（推荐）全部完成，P2（加分项）全部完成。

| 级别 | 功能项 | 状态 |
|------|--------|------|
| P0 必做 | 商品素材上传 / 剧本生成 / 基础分镜 / 一键成片 / 任务进度 / 预览导出 | ✅ 全部完成 |
| P1 推荐 | 素材标签+Embedding 检索 / 智能剪辑 Agent / 分镜级编辑 / TTS+字幕+BGM / 失败重试 / 生成过程 Trace / Mock 数据看板 | ✅ 全部完成 |
| P2 加分 | 多因子归因 / Agent 编排 / A/B 对比 / CI/CD / 可观测性 / 长任务体验 / 合规审核流 | ✅ 全部完成 |

### 项目亮点 / 创新点

1. **多模板 + 因子解耦的剧本引擎**：将视频创作方法论抽象为"策略（创作抽象方法）+ 因子（具体执行手段）+ 约束规则"三层解耦结构，支持动态组合生成多风格差异化剧本。3 套内置模板 × 6 维创作因子（开场/BGM/运镜/视觉重点/旁白/节奏）= 可扩展的剧本空间。

2. **分镜级 AI 干预与局部重生成**：区别于"整片重来"的传统流程，支持对单个分镜输入自然语言指令进行 AI 重生成，且改动不扩散到其他分镜。配合 Canvas 实时预览和拖拽排序，实现了接近专业剪辑工具的分镜编辑体验。

3. **全链路可观测 Trace + 多层 AI 兜底**：每次生成（剧本/视频）完整记录模型端点、耗时、质量评分、是否触发兜底、Prompt 摘要等元数据，形成可审计的生成 Trace。火山引擎 → DeepSeek → 本地规则生成三层兜底，确保端到端链路在任何 API 状态下均可运行。

4. **合规审核嵌入生成流水线**：在素材入库、剧本生成、视频输出三个关键节点嵌入合规检查（敏感词扫描、声明完整性校验、分辨率合规），审核结果可追溯，将合规从"事后审查"前置为"事中拦截"。

---

## 六、选填补充材料

### 产品截图/页面图集

（建议补充以下关键页面截图，附在飞书文档中）

| 页面 | 截图说明 |
|------|---------|
| 工作台 (Dashboard) | 统计卡片 + 风格×转化矩阵 + 多因子归因分析 + 热力图 |
| 素材管理 (Materials) | 素材表格 + 搜索过滤 + 上传弹窗 + 预览详情 |
| 剧本编辑 (ScriptEditor) | 剧本生成表单 + A/B 三模板对比卡片 + 分镜编辑器双面板 |
| 视频创作 (VideoStudio) | 项目列表 + 一键成片弹窗 + 分镜详情 + Canvas 预览 |
| 任务中心 (TaskCenter) | 任务卡片 + 进度条 + 步骤时间线 + 失败错误信息 |
| 方法论文库 (Methodology) | 爆款拆解卡片 + 模板详情 + 生成 Trace 表格 |
| 一键创建 (QuickCreate) | 端到端表单 + 5 步进度时间线 + 结果展示 |

### 接口文档 / API 清单

| 路由前缀 | 端点 | 方法 | 说明 |
|---------|------|------|------|
| `/api/materials` | `/` | GET | 素材列表（支持 category/keyword/tags/page/pageSize 过滤） |
| | `/` | POST | 上传素材（multipart/form-data，支持 20 文件/100MB） |
| | `/tags` | GET | 获取所有标签 |
| | `/:id` | GET | 获取单个素材 |
| | `/:id` | DELETE | 删除素材 |
| `/api/scripts` | `/` | GET | 剧本列表 |
| | `/generate` | POST | 生成单个剧本 |
| | `/generate-ab` | POST | A/B 多模板并行生成 |
| | `/templates` | GET | 获取所有模板 |
| | `/:id` | GET | 获取单个剧本 |
| | `/:id` | PATCH | 更新剧本 |
| | `/:id/shots/:shotId/regenerate` | POST | 单分镜 AI 重生成 |
| | `/:id` | DELETE | 删除剧本 |
| `/api/videos` | `/` | GET | 视频项目列表 |
| | `/` | POST | 创建视频项目（一键成片） |
| | `/create-ab` | POST | A/B 并行创建视频 |
| | `/:id` | GET | 获取视频项目 |
| | `/:id/shots/:shotId/rerender` | POST | 单分镜重渲染 |
| | `/:id` | DELETE | 删除视频项目 |
| `/api/tasks` | `/` | GET | 任务列表（支持 type 过滤） |
| | `/:id` | GET | 单个任务详情 |
| `/api/dashboard` | `/` | GET | 看板聚合指标 |
| `/api/agent` | `/pipeline` | POST | 创建 Agent 管线 |
| | `/pipeline/:id` | GET | 获取管线状态 |
| | `/pipelines` | GET | 管线列表 |
| | `/agents` | GET | Agent 角色列表 |
| `/api/compliance` | `/materials/:id` | POST | 素材合规检查 |
| | `/scripts/:id` | POST | 剧本合规检查 |
| | `/videos/:id` | POST | 视频合规检查 |
| | `/audit-log` | GET | 审核日志 |
| `/api/references` | `/` | GET | 爆款拆解参考（支持 category 过滤） |
| `/api/traces` | `/` | GET | 生成 Trace 列表（支持 type/page/pageSize） |
| `/api/health` | `/` | GET | 健康检查 |
| `/api/auth` | `/session` | POST | 访问令牌校验 |

### Prompt 策略 / Agent 流程图

**Agent 管线流程**：
```
素材分析 Agent → 剧本校验 Agent → 视觉匹配 Agent → 智能剪辑 Agent → 音频合成 Agent → 质量审核 Agent
     │                  │                  │                  │                  │                  │
  识别主体/类目      校验叙事结构        匹配素材切片        拼接+转场+字幕      BGM+配音合成      合规+质量评分
```

**剧本生成 Agent 流程**：
```
商品信息 + 策略因子 + 模板选择
        │
        ▼
  System Prompt (~40 约束) + User Prompt (结构化商品信息)
        │
        ▼
  LLM (Seed 2.0 Pro / DeepSeek)
        │
        ▼
  JSON Parsing → normalizeShots() → sanitizeVisualDescription() → evaluateStoryboard()
        │
        ▼
  Storyboard (4 shots, 15-20s, quality score)
```

### 评测方案与样例结果

**剧本质量评估**：内置 8 点自动评估 Checklist（`evaluateStoryboard()`），每项 pass/fail + 证据：
1. 时长结构合规（15-20s / 4 分镜）
2. 开场 Hook 强度（前 3 秒有吸引点）
3. 卖点展示清晰度
4. 信任背书镜头
5. CTA 合规性
6. 视觉描述无文字诱导
7. 合规风险可控
8. 素材/来源声明完备

**样例结果**：可在系统中实际操作体验，从素材上传到视频导出完整生成样例视频。

### 商业化 / 场景落地设想

- **目标客户**：TikTok Shop 跨境商家（尤其是中小商家，缺乏专业视频制作能力）
- **落地场景**：商家拍摄商品白底图/简短视频 → 上传系统 → 选择营销策略模板 → 1 分钟内获得可投放的带货短视频
- **增长路径**：单品多模板 A/B 测试 → 投流数据回流 → 因子归因分析 → 自动优化剧本策略 → 形成"生成→投放→分析→优化"闭环
- **扩展方向**：Prompt 市场（商家共享/交易高效 Prompt）、爆款视频 DNA 提取与复用、基于评论区/弹幕的二次创作

### 开发里程碑 / 版本迭代记录

| 阶段 | 内容 | 提交记录 |
|------|------|---------|
| v1.0 基础架构 | 前后端项目初始化、路由设计、素材管理 | Initial commit (925791f) |
| v2.0 核心功能 | 剧本生成引擎、视频渲染管线、任务系统、Dashboard | Merge (36d9440) |
| v3.0 完整交付 | A/B 对比、Agent 管线、合规审核、Trace 追踪、QuickCreate、Methodology、CI/CD、分镜编辑器增强 | Update (2327547) |

---

*本文档按照「AI 全栈挑战赛 — 项目成果内容标准和要求」编写，覆盖必填项与选填项全部字段。*
